---
- hosts:production

- name: User creation
  tasks:  
  -name: Test User
        shell: useradd -u test 
        when: ansible_user=!test


        #testing git 
        #modified the file from git
        
